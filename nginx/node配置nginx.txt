upstream paywz {
  server 127.0.0.1:3000;
}

server {
  listen  80;
  server_name 47.107.52.138;

  location / {
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forward $proxy_add_x_forwarded_for;
  proxy_set_header Host $http_host;
  proxy_set_header X-Nginx-Proxy true;

  proxy_pass http://paywz;
  proxy_redirect off;
}
}

server {
  listen  80;
  server_name 47.107.52.138;
  #rewrite ^(.*)https://$host$1 permanent;
  return 301 https://jdbook.paywz.cn$request_uri;
}
server {
  listen 443;
  server_name jdbook.paywz.cn;
  ssl on;
  root html;
  index index.html index.htm;
  ssl_certificate   /www/jdbook.paywz.cn.pem;
  ssl_certificate_key  /www/jdbook.paywz.cn.key;
  ssl_session_timeout 5m;
  ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;  ssl_protocols TLSv1 TLSv1.1 TLSv1.2;  ssl_prefer_server_ciphers on;    if ($ssl_protocol = "") {
    rewrite ^(.*) https://$host$1 permanent;
}   
  location / {
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forward $proxy_add_x_forwarded_for;
  proxy_set_header Host $http_host;
  proxy_set_header X-Nginx-Proxy true;
  
  proxy_pass http://jdbook.paywz;
  proxy_redirect off;
} 
} 


